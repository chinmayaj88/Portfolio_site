# ============================================
# Portfolio Website - Environment Variables
# ============================================
# Copy this file to .env.local for local development
# Or use these variables in your Docker deployment

# ============================================
# Application Configuration
# ============================================

# Production site URL (required for SEO, Open Graph, etc.)
# For local development: http://localhost:3000
# For production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Node environment
# Options: development, production, test
NODE_ENV=development

# ============================================
# Docker Configuration (for docker-compose)
# ============================================

# Docker Hub credentials (optional for public repos)
# Used for pulling images from private repositories
# DOCKER_USERNAME=your-dockerhub-username
# DOCKER_PASSWORD=your-dockerhub-password-or-token

# ============================================
# OCI VM Deployment (for manual deployment)
# ============================================

# OCI VM connection details (used by deployment scripts)
# OCI_HOST=123.456.789.0
# OCI_USERNAME=opc
# OCI_SSH_PORT=22

# ============================================
# Next.js Configuration
# ============================================

# Disable Next.js telemetry (optional)
NEXT_TELEMETRY_DISABLED=1

# ============================================
# Production Build Variables
# ============================================
# These are typically set during build time in CI/CD
# You don't need to set them manually for local development

# PORT=3000
# HOSTNAME=0.0.0.0

# ============================================
# Notes
# ============================================
# - NEXT_PUBLIC_* variables are exposed to the browser
# - Variables without NEXT_PUBLIC_ are server-side only
# - Never commit .env.local or .env.production files
# - For Docker: Set these as environment variables in docker-compose.yml
#   or pass them via -e flag: docker run -e NEXT_PUBLIC_SITE_URL=...
